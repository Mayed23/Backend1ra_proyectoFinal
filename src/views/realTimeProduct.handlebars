<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
</head>
<body>
    
<div class="container">
    {{!-- Productos con websocket --}}
    <div class ="infoProd">
        <h1> Productos con Socket</h1>
            <div class="detalleProd">
            <table>
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Modelo</th>
                    <th>Precio</th>
                    <th>Imagen</th>
                    <th>Código</th>
                    <th>Categoría</th>
                    <th>Stock</th>
                    <th>Id</th>
                </tr>
            </thead>
            <tbody>
                {{#each products}}
                    <tr>
                        <td>{{this.name}}</td>
                        <td>{{this.model}}</td>
                        <td>{{this.price}}</td>
                        <td><img src="{{this.img}}" alt="Imagen Producto"></td>
                         <td>{{this.code}}</td>
                        <td>{{this.category}}</td>
                        <td>{{this.stock}}</td>                      
                        
                    </tr>
                {{/each}}
            </tbody>
        </table>
            </div>
        </div>
    <div class="formulario">
        <h1>Formulario de  Productos</h1>
        <form id="prod-form">
            <div class="inputs">
                <div> 
                <input type="text" id="name" autocomplete="off" placeholder="Ingrese Nombre" required>
                <input type="text" id="modelo" autocomplete="off" placeholder="Ingrese Modelo" required>
                <input type="text" id="codigo" autocomplete="off" placeholder="Ingrese Codigo" required>
                <input type="text" id="price" autocomplete="off" placeholder="Ingrese Precio" required>
            </div>
            <div>
                <input type="text" id="stock" autocomplete="off" placeholder="Ingrese Stock" required>
                <input type="text" id="category" autocomplete="off" placeholder="Ingrese Categoria" required>
                <input type="text" id="img" autocomplete="off" placeholder="Ingrese Imagen" required>
            </div>
            </div>
            <div class="botones">
                <button type="submit">Crear</button>
            </div>    
        </form>
    </div>
</div>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/realTimeProducts.js"></script>
</body>
</html>